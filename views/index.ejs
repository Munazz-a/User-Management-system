<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User management</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"><body>
    <div class="border-b py-3">
        <div class="container mx-auto flex justify-between items-center max-w-5xl px-4">
            <a href="/" class="font-semibold">User management</a>
            <a href="/add" class="font-sm hover:text-blue-600">Add user</a>
        </div>

    </div>

    <div class="mt-44 max-w-md mx-auto">
        <% if(userCount === 0){ %>
            <h1 class="font-bold text-lg text-center">No User is available</h1>
            <p class="text-sm text-neutral-600 mb-3 text-center">Please add a user.</p>
            <div class="flex justify-center"> <a href="/add" class="bg-black text-white p-2 mt-3 items-center">Add User</a> </div>

        <%} else{%>
            <h1 class="font-bold text-lg text-center"> Manage User</h1>
            <p class="text-sm text-neutral-600 mb-3 text-center mb-8">Please select a user.</p>

            <% for(let i = 0; i < users.length; i++){ %>
                
                <div class="flex justify-between items-center mt-2 p-2 border-b">

                    <div class="flex gap-3 items-center mr-4">
                        <img src="<%= users[i].img %>" class="w-12 h-12 rounded-full object-cover flex-shrink-0" alt="User Image">
                        <div class="font-sm"> <%= users[i].name %> </div>
                        <div class="font-sm"> <%= users[i].email %> </div>
                    </div>
                    <div class="flex gap-4 items-center justify-center">
                        
                        <form action="/edit" method="post">
                            <input type="hidden" name="userId" value="<%= i %>">
                            <button type="submit"> <i class="bi bi-pencil"></i> </button>
                        </form>
                        <form action="/delete" method="post">
                            <input type="hidden" name="userId" value="<%= i %>">
                            <button type="submit"> <i class="bi bi-trash3"></i> </button>
                        </form>
                    </div>
                </div>

                

            <%}%>
            <div class="flex justify-center mt-5">
                <a href="/add" class="bg-black text-white p-2 rounded">Add User</a>
            </div>
        <%}%>

        
    </div>

    

</body>
</html>